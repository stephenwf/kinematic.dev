import styled from 'styled-components';

export const TreeNodeLabel = styled.div`
  flex: 1 1 0px;
  text-overflow: ellipsis;
  overflow-x: hidden;
  white-space: nowrap;
  padding-left: 2px;
  align-self: center;
  user-select: none;
  font-size: 14px;
  line-height: 20px;
`;
